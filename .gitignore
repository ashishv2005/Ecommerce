# Node dependencies
node_modules/

# Build output (TypeScript compiled JS files)
dist/
build/

# Environment variables (must never be pushed)
.env
.env.local
.env.production
.env.development

# Sequelize auto-generated files (optional)
*.sqlite
*.sql

# Redis dump files (in case Redis writes local snapshot)
dump.rdb

# TypeScript cache
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS & Editor garbage files
.DS_Store
Thumbs.db

# Editor configs (not needed in repo)
.vscode/
.idea/

# Temporary folders
temp/
tmp/
*.tmp

# Test coverage (if you add tests in future)
coverage/

# Uploads folder (You use URL-based images, but keeping for future use)
uploads/
